# Implementation summary — AKR-QVAC-TOOL-LOOP

Runner executes an allowlisted tool loop with iteration/call limits,
controlled unknown-tool errors, and untrusted DATA framing before the
final structured JSON turn.

